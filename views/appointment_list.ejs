<!DOCTYPE html>
<html lang="en">
<head>
    <% include template/head.ejs %>
</head>
<body>
<div class="container">
    <% include template/errors.ejs %>
    <% include template/sidebar.ejs %>
    <h1><%= pageTitle %></h1>
    <br><br>
    <br><br>
    <div id="available_timeslot">
        <select>
            <option selected value="">select times</option>
            <%available_times.forEach(function(timebox){%>
                <option> <%=timebox.from%> - <%=timebox.to%></option>
            <%})%>
        </select>

        <table id="browse_timetable" style="display:none">
            <tr class="header">
                <th style="width:50%;">F</th>
                <th style="width:50%;">Name</th>
            </tr>
            <%if(typeof client_list !== 'undefined'){
            client_list.forEach(function(client){ %>
            <%if(typeof client.id !=='undefined'){%>
            <tr class="client_url" data-href="<%='clients/'+client.id%>">
                <td><%=client.id%></td>
                <td><%=client.name%></td>
                <td><%=client.date_of_birth%></td>
            </tr>
            <% }});
            }%>
        </table>
    </div>
</div>

</body>
</html>